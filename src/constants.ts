export enum Input {
    CacheKey = "cache-key",
    AppendTimestamp = "append-timestamp",
    ConfigPath = "config",
    RemotePatterns = "remotes",
    SaveCache = "save",
    Lockfile = "lockfile",
}

export enum State {
    PrimaryCacheHit = "PrimaryCacheHit",
}
